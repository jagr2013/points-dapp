# Stacks Simple Points DApp

Minimal on-chain points tracking contract on Stacks blockchain.

## Repository Structure


## Contract

- `points.clar` — simple points contract
- Public functions:
  - `add-points(amount)` → add points to caller
  - `deduct-points(amount)` → deduct points from caller
  - `get-points(user)` → check user points
